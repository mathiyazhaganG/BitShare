<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bitshare - Password Protected File</title>
  <style>
    :root {
      --primary: #3a86ff;
      --primary-dark: #2667cc;
      --secondary: #8338ec;
      --success: #06d6a0;
      --warning: #ffbe0b;
      --danger: #ef476f;
      --dark: #1a1a2e;
      --light: #f8f9fa;
      --gray: #e9ecef;
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    
    body {
      background-color: var(--light);
      color: var(--dark);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
    
    .container {
      width: 90%;
      max-width: 500px;
      padding: 2.5rem;
      background: white;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    }
    
    header {
      text-align: center;
      margin-bottom: 2rem;
    }
    
    .logo {
      font-size: 2.5rem;
      font-weight: 800;
      color: var(--primary);
      letter-spacing: -1px;
      margin-bottom: 0.5rem;
    }
    
    .tagline {
      font-size: 1rem;
      color: #666;
      margin-bottom: 1rem;
    }
    
    .lock-icon {
      font-size: 3rem;
      color: var(--warning);
      margin-bottom: 1rem;
      display: block;
    }
    
    .form-container {
      margin-top: 1rem;
    }
    
    .error-message {
      background-color: rgba(239, 71, 111, 0.1);
      border-left: 4px solid var(--danger);
      color: var(--danger);
      padding: 1rem;
      margin-bottom: 1.5rem;
      border-radius: 4px;
      font-weight: 500;
    }
    
    .form-group {
      display: grid;
      gap: 0.5rem;
      grid-template-columns: auto 1fr;
      align-items: center;
      margin-bottom: 1rem;
    }
    
    label {
      font-weight: 600;
      color: var(--dark);
    }
    
    .password-input {
      width: 100%;
      padding: 0.8rem 1rem;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 1rem;
      transition: all 0.3s;
    }
    
    .password-input:focus {
      outline: none;
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(58, 134, 255, 0.25);
    }
    
    .submit-btn {
      grid-column: span 2;
      background: linear-gradient(45deg, var(--primary), var(--secondary));
      color: white;
      border: none;
      border-radius: 6px;
      padding: 0.8rem;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: transform 0.3s, box-shadow 0.3s;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
    }
    
    .submit-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 8px rgba(0, 0, 0, 0.15);
    }
    
    .submit-btn:active {
      transform: translateY(0);
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    
    .file-info {
      margin-top: 1.5rem;
      background-color: var(--gray);
      padding: 1rem;
      border-radius: 6px;
      font-size: 0.9rem;
      color: #666;
    }
    
    footer {
      margin-top: 2rem;
      text-align: center;
      font-size: 0.8rem;
      color: #666;
    }
    
    @media (max-width: 500px) {
      .form-group {
        grid-template-columns: 1fr;
      }
      
      .submit-btn {
        grid-column: 1;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1 class="logo">Bitshare</h1>
      <p class="tagline">Password Protected File</p>
      <span class="lock-icon">🔒</span>
    </header>
    
    <div class="form-container">
      <% if(locals.error === true) { %>
      <div class="error-message">
        Incorrect Password. Please try again.
      </div>
      <% } %>
      
      <!-- Important: Keeping the original form structure while adding styling -->
      <form action="/file/<%= locals.fileId %>" method="post" style="display: grid; gap: .5rem; grid-template-columns: auto 1fr; max-width: 500px; margin: 0 auto;">
        <label for="password">Password:</label>
        <input type="password" id="password" name="password" required class="password-input" />
        <button style="grid-column: span 2;" type="submit" class="submit-btn">Download</button>
      </form>
      
      <div class="file-info">
        This file is protected with a password. Enter the correct password to download the file.
      </div>
    </div>
    
    <footer>
      <p>© 2025 Bitshare</p>
    </footer>
  </div>
</body>
</html>